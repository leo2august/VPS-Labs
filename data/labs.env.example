# =========================================================
# VPS Labs — environment template (copy to data/labs.env)
# All values are placeholders. Replace before first run.
# =========================================================
LABS_USER=admin
LABS_PASSWORD=ChangeMe_Strong_123
# Session signing secret — generate with: openssl rand -hex 32
LABS_SECRET=REPLACE_WITH_64_HEX_CHARS
# 1 if served over HTTPS (recommended), 0 only for plain HTTP
LABS_SECURE_COOKIE=1
